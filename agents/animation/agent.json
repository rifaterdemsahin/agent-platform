{
  "name": "animation",
  "version": "1.0.0",
  "description": "Generates storyboards, selects animation libraries, and coordinates video rendering sub-agents.",
  "runtime": { "type": "node", "llm": "claude-sonnet-4-20250514", "background": true, "pollingIntervalMs": 60000 },
  "skills": [
    { "name": "hello", "description": "Basic identity check — returns a greeting confirming the agent is running", "parameters": { "name": "string — optional name" } },
    { "name": "verify", "description": "Rules check — evaluate an expression to verify agent integrity", "parameters": { "expression": "string — optional JS expression to evaluate" } },
    { "name": "generate-storyboard", "description": "Generate a 4-panel animation storyboard from a description", "parameters": { "description": "string", "style": "string" } },
    { "name": "select-library", "description": "Recommend the best animation library for a target platform", "parameters": { "targetPlatform": "string", "complexity": "string", "performanceBudget": "string" } }
  ],
  "subAgents": [
    { "name": "remotion", "path": "remotion", "description": "React-based video rendering with Remotion: compositions, sequences, audio, transitions, overlays, Lambda deploy" },
    { "name": "lottie", "path": "lottie", "description": "Lottie/Bodymovin vector animation generation, optimization, and React integration" }
  ],
  "entrypoint": "index.js",
  "triggers": ["on-demand"],
  "api": { "invokeUrl": "/api/agents/animation/invoke", "skillsUrl": "/api/agents/animation/skills" }
}
