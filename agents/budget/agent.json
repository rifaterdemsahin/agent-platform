{
  "name": "budget",
  "version": "1.0.0",
  "description": "Tracks LLM usage costs, enforces budget limits, and provides cost optimization recommendations.",
  "runtime": { "type": "node", "llm": "claude-sonnet-4-20250514", "background": true, "pollingIntervalMs": 60000 },
  "skills": [
    { "name": "hello", "description": "Basic identity check — returns a greeting confirming the agent is running", "parameters": { "name": "string — optional name" } },
    { "name": "verify", "description": "Rules check — evaluate an expression to verify agent integrity", "parameters": { "expression": "string — optional JS expression to evaluate" } },
    { "name": "set-budget", "description": "Set a budget limit for a project", "parameters": { "projectId": "string", "limit": "number", "period": "string", "alertThreshold": "number" } },
    { "name": "get-status", "description": "Get budget status for a project", "parameters": { "projectId": "string" } }
  ],
  "subAgents": [
    { "name": "llm-budget", "path": "llm-budget", "description": "LLM cost estimation, usage tracking, optimization, and forecasting" },
    { "name": "personal-budget", "path": "personal-budget", "description": "Personal expense tracking, savings goals, and monthly reporting" }
  ],
  "entrypoint": "index.js",
  "triggers": ["scheduled", "on-demand", "threshold-alert"],
  "api": { "invokeUrl": "/api/agents/budget/invoke", "skillsUrl": "/api/agents/budget/skills" }
}
