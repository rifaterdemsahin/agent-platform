{
  "name": "budget",
  "version": "1.0.0",
  "description": "A background agent that monitors costs, estimates LLM token usage, tracks compute spend, and alerts when budgets are exceeded. Works across projects and deployments.",
  "runtime": {
    "type": "node",
    "llm": "claude-sonnet-4-20250514",
    "background": true,
    "pollingIntervalMs": 60000
  },
  "skills": [
    {
      "name": "estimate-cost",
      "description": "Estimates the cost of an LLM call based on model, token count, and provider pricing",
      "parameters": {
        "model": "string — model identifier",
        "inputTokens": "number",
        "outputTokens": "number",
        "provider": "string — 'anthropic' | 'openai' | 'google'"
      }
    },
    {
      "name": "track-usage",
      "description": "Tracks cumulative token usage and cost for a project or agent over time",
      "parameters": {
        "projectId": "string",
        "period": "string — 'daily' | 'weekly' | 'monthly' | 'total'"
      }
    },
    {
      "name": "set-budget",
      "description": "Sets a budget threshold and returns alert configuration",
      "parameters": {
        "projectId": "string",
        "limit": "number — maximum cost in USD",
        "period": "string — 'daily' | 'weekly' | 'monthly'",
        "alertThreshold": "number — percentage of limit that triggers alert"
      }
    },
    {
      "name": "optimize-costs",
      "description": "Analyzes usage patterns and suggests cost optimizations (model changes, prompt shortening, caching)",
      "parameters": {
        "projectId": "string"
      }
    },
    {
      "name": "forecast",
      "description": "Projects future costs based on historical usage trends",
      "parameters": {
        "projectId": "string",
        "daysAhead": "number"
      }
    }
  ],
  "entrypoint": "index.js",
  "triggers": ["scheduled", "on-demand", "threshold-alert"],
  "api": {
    "invokeUrl": "/api/agents/budget/invoke",
    "skillsUrl": "/api/agents/budget/skills",
    "statusUrl": "/api/agents/budget/status"
  }
}
